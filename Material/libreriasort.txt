/*
#include <iostream>
#include <vector>
#include <algorithm>
#include <list>
#include <string>
using namespace std;

int main()
{
    vector<int> numeros = {5, 2, 9, 1, 7};

    // Ordenar de menor a mayor
    sort(numeros.begin(), numeros.end());

    cout << "Vector ordenado:\n";
    for (int num : numeros)
    {
        cout << num << " ";
    }

    return 0;
}
---------------------------------------------------------------------------------------------------------------
int main() {
    list<string> nombres = {"Carlos", "Ana", "Luis", "Beatriz"};

    // Ordenar alfabéticamente
    nombres.sort();

    cout << "Lista ordenada de nombres:\n";
    for (const auto& nombre : nombres) {
        cout << nombre << "\n";
    }

    return 0;
}

---------------------------------------------------------------------------------------------------------------

void ordenar(list<Biblioteca> &biblioteca)
{
    Se ordenan por nombre de biblioteca se usa esta estructura al ser una lista

    biblioteca.sort([](const Biblioteca &nuevo, const Biblioteca &nueva)
                    { return nuevo.Nombre < nueva.Nombre; });
    cout << "Biblioteca ordenada" << endl;

-----------------------------------------------------------------------------------------------------------------
    // se ordenan los libros por año de publicacion
    for (auto &biblio : biblioteca)
    { // aca se declaran las variables
        sort(biblio.coleccion.begin(), biblio.coleccion.end(), [](const Libro &a, const Libro &b)

             { return a.Ano_Publicacion < b.Ano_Publicacion; });
    }
    cout << "Libros ordenados por año de publicación en cada biblioteca" << endl;
}
---------------------------------------------------------------------------------------------------------------------------

int main() {
    list<int> numeros = {5, 2, 8, 1, 3};

    numeros.sort(); // Ordena de menor a mayor

    cout << "Lista ordenada de números:\n";
    for (int num : numeros) {
        cout << num << " ";
    }

    return 0;
}
--------------------------------------------------------------------------------------------------------------------------
Objetos personalizados como los struct


struct Persona {
    string nombre;
    int edad;
};
// Ordenar por edad
lista.sort([](const Persona &a < b.edad);

{});

*/
-------------------------------------------------------------------------------------------------------------------
/*

void solicitarInformacion(list<Biblioteca> &bibliotecas)
{
    int cantidadBibliotecas;
    cout << "¿Cuántas bibliotecas desea registrar? ";
    cin >> cantidadBibliotecas;
    cin.ignore(); // limpia el buffer

    for (int i = 0; i < cantidadBibliotecas; i++)
    {
        Biblioteca b;
        cout << "\n=== Biblioteca #" << (i + 1) << " ===" << endl;
        cout << "Ingrese el nombre de la biblioteca: ";
        getline(cin, b.nombre);

        cout << "Ingrese la ubicación de la biblioteca: ";
        getline(cin, b.ubicacion);

        int cantidadLibros;
        cout << "¿Cuántos libros tiene esta biblioteca? ";
        cin >> cantidadLibros;
        cin.ignore();

        for (int j = 0; j < cantidadLibros; j++)
        {
            Libro l;
            cout << "\n--- Libro #" << (j + 1) << " ---" << endl;
            cout << "Título: ";
            getline(cin, l.titulo);
            cout << "Autor: ";
            getline(cin, l.autor);
            cout << "Año de publicación: ";
            cin >> l.anioPublicacion;
            cout << "Número de páginas: ";
            cin >> l.paginas;
            cin.ignore();

            b.libros.push_back(l);
        }

        bibliotecas.push_back(b);
        cout << "\nBiblioteca registrada correctamente ✅\n";
    }
}
*/